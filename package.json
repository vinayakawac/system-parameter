{
  "name": "rxd-system-parameter",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server.mjs",
    "build": "node build.mjs",
    "check": "node --check app.js && node --check data.js && node --check server.mjs && node --check build.mjs"
  }
}
